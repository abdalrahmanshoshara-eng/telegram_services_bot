# دليل بناء وتشغيل بوت الخدمات على تلغرام

هذا الدليل يشرح خطوة بخطوة كيف تبني بوت تلغرام يعرض قائمة خدماتك (منصّاتك)، وعند اختيار المستخدم لخدمة يعرض له وصفاً مختصراً مع زر ينقله مباشرةً إلى المنصّة. ستشغّله الآن على لابتوبك، ولاحقاً على سيرفر VPS ليعمل 24 ساعة.

الملفات المرفقة معك:

- `bot.py` — كود البوت الجاهز.
- `config.py` — الإعدادات: التوكن وقائمة المنصّات (هنا تضيف منصّاتك المستقبلية).
- `requirements.txt` — المكتبات المطلوبة.

---

## الخطوة 1 — إنشاء البوت والحصول على التوكن (Token)

1. افتح تلغرام وابحث عن `@BotFather` (الحساب الرسمي، عليه علامة توثيق زرقاء).
2. اضغط **Start**، ثم أرسل الأمر: `/newbot`
3. سيطلب منك اسماً للبوت (Name) — مثل: `خدمات وزارة الداخلية`.
4. ثم اسماً مستخدماً (Username) — يجب أن ينتهي بكلمة `bot`، مثل: `moid_services_bot`.
5. سيعطيك **التوكن** وهو نص طويل مثل:
   `123456789:AAExxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

> ⚠️ **مهم:** هذا التوكن سرّي تماماً. من يملكه يتحكّم بالبوت بالكامل. لا تشاركه مع أحد ولا تنشره في مكان عام.

**اختياري** (لتحسين شكل البوت) — من داخل BotFather:
- `/setdescription` — تكتب وصفاً للبوت.
- `/setuserpic` — ترفع صورة للبوت.
- `/setcommands` — تضيف الأوامر لتظهر كقائمة، الصق التالي:
  ```
  start - بدء استخدام البوت وعرض الخدمات
  services - عرض قائمة الخدمات
  ```

---

## الخطوة 2 — تثبيت Python على اللابتوب

### إذا كان نظامك Windows:
1. حمّل Python من الموقع الرسمي: <https://www.python.org/downloads/>
2. شغّل ملف التثبيت، وعند أول شاشة **ضع علامة ✔ على خيار `Add Python to PATH`** (مهم جداً)، ثم اضغط Install.
3. للتأكد من نجاح التثبيت، افتح **Command Prompt** (اكتب `cmd` في قائمة ابدأ) واكتب:
   ```
   python --version
   ```
   يجب أن يظهر رقم الإصدار (مثل 3.12).

### إذا كان نظامك macOS أو Linux:
غالباً Python مثبّت مسبقاً. تأكد بكتابة `python3 --version` في الطرفية.

---

## الخطوة 3 — وضع ملفات البوت وتثبيت المكتبات

1. أنشئ مجلداً على اللابتوب باسم `telegram_services_bot`، وضع بداخله الملفات الثلاثة:
   `bot.py` و `config.py` و `requirements.txt`.
2. افتح موجّه الأوامر داخل هذا المجلد:
   - **Windows:** افتح المجلد، ثم في شريط العنوان اكتب `cmd` واضغط Enter.
3. ثبّت المكتبة المطلوبة:
   ```
   pip install -r requirements.txt
   ```
   (على macOS/Linux استخدم `pip3` بدل `pip`).

---

## الخطوة 4 — وضع التوكن

افتح ملف `config.py` بأي محرّر نصوص (مثل Notepad أو VS Code)، وابحث عن السطر:

```python
BOT_TOKEN = os.environ.get("BOT_TOKEN", "ضع_توكن_البوت_هنا")
```

استبدل `ضع_توكن_البوت_هنا` بالتوكن الذي أعطاك إياه BotFather، مع إبقاء علامتَي التنصيص، هكذا:

```python
BOT_TOKEN = os.environ.get("BOT_TOKEN", "123456789:AAExxxxxxxxxxxxxxxxxxxxx")
```

احفظ الملف.

---

## الخطوة 5 — تشغيل البوت على اللابتوب (تجربة)

في موجّه الأوامر داخل المجلد، اكتب:

```
python bot.py
```

(على macOS/Linux: `python3 bot.py`).

إذا ظهرت رسالة `البوت يعمل الآن...` فهذا يعني نجاح التشغيل. الآن افتح تلغرام، ادخل على بوتك، وأرسل `/start`. ستظهر قائمة الخدمات؛ اضغط على أي خدمة فيظهر وصفها مع زر **«الدخول إلى المنصة»** الذي يفتح رابط المنصّة.

> لإيقاف البوت اضغط `Ctrl + C` في نافذة الأوامر.

⚠️ **ملاحظة:** طالما تشغّل البوت من اللابتوب، فهو يعمل فقط ما دامت نافذة الأوامر مفتوحة واللابتوب موصولاً بالإنترنت. بمجرّد إغلاق النافذة أو إطفاء اللابتوب يتوقّف البوت. لهذا نحتاج الخطوة التالية (VPS) لجعله يعمل 24 ساعة.

---

## الخطوة 6 — إضافة منصّات جديدة مستقبلاً (سهلة جداً)

كل ما عليك فعله هو فتح `config.py` وإضافة عنصر جديد داخل قائمة `SERVICES`. انسخ القالب الموجود (المُعلّق عليه بـ `#`) وعدّل قيمه:

```python
{
    "id": "newplatform",              # معرّف فريد بدون مسافات
    "title": "اسم المنصة الجديدة",
    "emoji": "🌐",
    "desc": "وصف قصير للمنصة.\n\nاضغط الزر بالأسفل للدخول.",
    "url": "https://example.moid.gov.sy/login",
},
```

احفظ الملف ثم أعد تشغيل البوت، وستظهر المنصة الجديدة تلقائياً كزر في القائمة. لا حاجة لتعديل `bot.py` إطلاقاً.

---

## الخطوة 7 — تشغيله 24 ساعة على سيرفر VPS (مستقبلاً)

عندما تنقل البوت إلى سيرفر Linux (مثل الذي عليه منصّاتك)، اتبع التالي حتى يعمل باستمرار ويعيد تشغيل نفسه تلقائياً عند إعادة تشغيل السيرفر.

### 7-أ) رفع الملفات وتجهيز البيئة
اتصل بالسيرفر عبر SSH، ثم:

```bash
sudo apt update && sudo apt install -y python3 python3-pip python3-venv
mkdir -p /opt/telegram_bot && cd /opt/telegram_bot
# انسخ ملفات bot.py و config.py و requirements.txt إلى هذا المجلد
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

جرّب التشغيل مرّة للتأكد: `python bot.py` ثم أوقفه بـ `Ctrl+C`.

### 7-ب) جعله خدمة دائمة عبر systemd
أنشئ ملف الخدمة:

```bash
sudo nano /etc/systemd/system/telegram-bot.service
```

الصق بداخله التالي (عدّل التوكن ومسار المجلد إن لزم):

```ini
[Unit]
Description=Telegram Services Bot
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/telegram_bot
Environment=BOT_TOKEN=123456789:AAExxxxxxxxxxxxxxxxxxxxx
ExecStart=/opt/telegram_bot/venv/bin/python /opt/telegram_bot/bot.py
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

> بهذه الطريقة يُقرأ التوكن من `Environment` في ملف الخدمة، ويمكنك ترك `config.py` بقيمته الافتراضية.

ثم فعّل الخدمة وشغّلها:

```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
```

### 7-ج) متابعة الحالة والسجلات
```bash
sudo systemctl status telegram-bot      # حالة البوت
sudo journalctl -u telegram-bot -f      # مشاهدة السجل مباشرةً
sudo systemctl restart telegram-bot     # إعادة التشغيل بعد أي تعديل
```

بهذا يعمل البوت 24 ساعة، ويعيد تشغيل نفسه تلقائياً إذا تعطّل أو أُعيد إقلاع السيرفر.

---

## ملاحظات وأمان

- **سرّية التوكن:** لا ترفع `config.py` وبداخله التوكن إلى مستودع عام (GitHub). على السيرفر استخدم طريقة `Environment` أعلاه.
- **مكان تشغيل واحد فقط:** لا تشغّل نفس البوت (بنفس التوكن) على اللابتوب والسيرفر في آنٍ واحد؛ سيتعارضان. أوقف نسخة اللابتوب قبل تشغيل نسخة السيرفر.
- **الروابط فقط:** هذا البوت لا يطلب بيانات المستخدم ولا كلمات المرور؛ هو فقط يعرض الخدمات ويوجّه المستخدم إلى المنصّة الرسمية، وهذا هو الأسلوب الآمن والصحيح.
- **الأوامر المتاحة للمستخدم:** `/start` لبدء الاستخدام، و `/services` لعرض القائمة في أي وقت.
